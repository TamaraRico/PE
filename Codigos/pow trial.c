 #include<stdio.h>
 #include<math.h>

 int main(void)
 {
    int i, b,grad=3, num=1, arreglo[]={1, 2, 3, 4};
    float a;
    /*for(i=0; i<=grad; i++)
    {
        printf("grado=", grad);
        a=pow(arreglo[0],grad);
        printf("a=", a);
        grad-=1;

    }*/
    b=arreglo[2];
    a=pow(2,5);
    printf("final %d", a);
    return 0;
 }
